[build]
public_url = "./"
dist = "dist"

[serve]
addresses = [ "0.0.0.0" ]
port = 8080
open = true

[watch]
watch = ["src", "Cargo.toml", "index.html"]

ignore = [
  "dist",
  "target",
  "result",
  "flake.nix",
  "flake.lock",
]
